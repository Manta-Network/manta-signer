import { expect } from 'chai';
import { ApiPromise, WsProvider } from '@polkadot/api'

describe('Array', () => {
    describe('#indexOf()', () => {
        it('should return -1 ...', () => {
            expect(1).to.equal(1);
        });
    });
});
